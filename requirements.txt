# ==============================================================================
# BlindAssist — Raspberry Pi 5 Python Dependencies
# ==============================================================================
#
# NOTE: Many of these packages require system libraries. Run the system setup
# commands listed at the bottom of this file BEFORE running pip install!
#
# Install with:
#   pip3 install -r requirements.txt
# ==============================================================================

# Core System & Orchestrator
pygame>=2.5.0               # Used for centralized TTS audio playback (pygame.mixer)
requests>=2.31.0            # Used for Nominatim & OSRM online API calls

# Vision & OCR Module
opencv-python-headless>=4.8.0 # OpenCV without GUI overhead (use opencv-python if drawing GUI is needed)
pytesseract>=0.3.10         # Wrapper for Tesseract OCR engine

# AI & LLM Query Module
google-generativeai>=0.4.0  # Google Gemini Pro / Flash API
# llama-cpp-python>=0.2.50  # Un-comment to install local LLM support (needs build-essential)

# Translation Module
googletrans==4.0.0-rc1      # Google Translate API

# Gesture Recognition Module
mediapipe>=0.10.0           # Hand Landmark and Gesture tracking (Tasks API)

# Emotion Engine Module
librosa>=0.10.1             # Audio feature extraction (MFCC)
scikit-learn>=1.3.0         # SVM Classifier
numpy>=1.24.0               # Numerical computations (used by Mediapipe, YOLO, and MFCCs)

# GPS Module
pynmea2>=1.18.0             # NMEA 0183 parser for GPS HAT sentences
pyserial>=3.5               # Serial port reader for Pi GPIO GPS connections (UART)

# Object Recognition Module
ultralytics>=8.1.0          # YOLOv8 object detection framework


# ==============================================================================
# 🛠️ REQUIRED RASPBERRY PI SYSTEM DEBIAN PACKAGES (APT)
# ==============================================================================
# Run the following commands on your Raspberry Pi:
#
# 1. Update packages:
#    sudo apt update && sudo apt upgrade -y
#
# 2. Install compiler tools and system development libraries:
#    sudo apt install -y build-essential python3-dev
#
# 3. Install OCR, TTS, audio playback, serial, and Librosa prerequisites:
#    sudo apt install -y tesseract-ocr espeak-ng libespeak-ng-dev libsndfile1 portaudio19-dev libasound2-dev
# ==============================================================================
